import numpy as np
np.random.seed(0)

if __name__ == "__main__":
    # A simple test to check if numpy is working correctly.
    # Now I work on MAIN branch which is not recommended.
    a = np.random.rand(5)
    print(a)